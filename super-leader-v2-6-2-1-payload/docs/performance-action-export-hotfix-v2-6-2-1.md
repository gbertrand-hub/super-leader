# Super Leader V2.6.2.1

This hotfix restores the complete V2.6 `src/app/actions/performance.ts` file.

It fixes the Next.js build error:

`Export reopenWorkdayAction doesn't exist in target module`

No database migration is required.
